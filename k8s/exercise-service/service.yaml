apiVersion: v1
kind: Service
metadata:
  name: exercise-service
  namespace: msa
spec:
  type: LoadBalancer
#  type: NodePort
  selector:
    app: exercise-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
#      nodePort: 30080
